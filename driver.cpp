//Matthew Julio
//This will compile and call the needed functions to put the summerized data into a file
//9-9-2020

#include "individuals.h"
#include "overall.h"
#include "historical.h"

#define MAX 80

using namespace std;

int main (arc, *arv[])
{
	float tempFloat;
	int tempInt;
	string tempString;
	individual people[MAX];
	group part[MAX];
	if(argc!=2)
	{
		cout << "error: improper use" << endl << "Peoper use:" << endl << "./meetingData Individual.txt Overall.txt" << endl;
		return 0,
	}
	scanIndividual(people[], argv[1])
}

int scanIndividual (individual people[], string fileName)
{
	
}

totalTime

shortest

longest

percentageSpeaking

